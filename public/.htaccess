<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteRule ^api/ - [L]

  RewriteCond %{REQUEST_URI} \.(js|mjs|css|map|png|jpe?g|gif|webp|svg|ico|txt|woff2?|ttf|eot)$ [NC]
  RewriteRule ^ - [L]

  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule . index.html [L]
</IfModule>
